# Traceback

`liblaf.traceback` presents raised exceptions as plain text or Rich renderables
without changing the exception itself. Start with [basic usage](getting-started/basic-usage.md),
then review [rendering policy](concepts/rendering-policy.md) before enabling locals in production.
