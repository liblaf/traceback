#:schema https://mise.jdx.dev/schema/mise.json
# This file is @generated by <https://github.com/liblaf/copier-python>.
# DO NOT EDIT!

[hooks.enter]
task = "install"

#
[tasks."docs:build"]
description = "Build the documentation site"
run = [
  # Clear the cache to force API reference regeneration when source code changes.
  # TODO: Remove this workaround once the upstream documentation tool updates the API reference automatically on source code changes.
  "rm --force --recursive '{{ config_root }}/.cache'",
  "zensical build",
]

[tasks."docs:serve"]
description = "Serve the documentation site locally"
run = ["rm --force --recursive '{{ config_root }}/.cache'", "zensical serve"]
#

[tasks."gen:ref-pages"]
description = "Generate API reference pages from public Python modules"
file = "https://cdn.jsdelivr.net/gh/liblaf/copier-python@c187fb7bd48daa8f9aa1f4292004de8f54ddc3b2/mise-tasks/gen/ref-pages.py"

[tasks.install]
description = "Install project dependencies from pixi.lock or uv.lock"
file = "https://cdn.jsdelivr.net/gh/liblaf/copier-python@c187fb7bd48daa8f9aa1f4292004de8f54ddc3b2/mise-tasks/install.sh"

[tasks.lint]
description = "Run all linters"
depends = ["lint:python", "lint:toml"]

[tasks."lint:python"]
description = "Lint and format Python files"
run = ["lint-imports", "ruff check --fix", "ruff format", "ty check --fix"]

[tasks."lint:toml"]
description = "Sort, format, and lint TOML files with toml-sort and tombi"
file = "https://cdn.jsdelivr.net/gh/liblaf/copier-python@c187fb7bd48daa8f9aa1f4292004de8f54ddc3b2/mise-tasks/lint/toml.sh"

[tasks.upgrade]
description = "Upgrade project dependencies from pixi.lock or uv.lock"
file = "https://cdn.jsdelivr.net/gh/liblaf/copier-python@c187fb7bd48daa8f9aa1f4292004de8f54ddc3b2/mise-tasks/upgrade.sh"
